<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wmang.logis.core.dao.user.SysUserExtMapper">
	<select id="validateLogin" resultType="java.lang.Integer">
		select count(1) from sys_user where account=#{account} and passwd=#{passwd}
	</select>
</mapper>